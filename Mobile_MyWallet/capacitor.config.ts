import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Mobile_MyWallet',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
